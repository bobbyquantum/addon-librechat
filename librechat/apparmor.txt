#include <tunables/global>

profile librechat flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  #include <abstractions/openssl> 

  # Capabilities
  file,
  signal (send) set=(kill,term,int,hup,cont),

  /etc/ssl/openssl.cnf r,
  /etc/ssl/certs/** r,
  /usr/lib/ssl/openssl.cnf r,

  /init ix,
  /bin/** ix,
  /usr/bin/** ix,
  /run/{s6,s6-rc*,service}/** ix,
  /package/** ix,
  /command/** ix,
  /etc/services.d/** rwix,
  /etc/cont-init.d/** rwix,
  /etc/cont-finish.d/** rwix,
  /run/{,**} rwk,
  /dev/tty rw,

  /usr/lib/bashio/** ix,
  /tmp/** rwk,

  /data/** rw,
  /share/** rw,
  /addon_config/** rw,

  /usr/local/bin/node ix,
  /usr/local/lib/node_modules/** rm,
  /opt/** rwix,

  # MongoDB
  /usr/bin/mongod cx -> mongodb,
  /var/lib/mongodb/** rwk,
  /var/log/mongodb/** rwk,

  # MeiliSearch
  /usr/bin/meilisearch cx -> meilisearch,
  /var/lib/meilisearch/** rwk,

  profile mongodb flags=(attach_disconnected,mediate_deleted) {
    #include <abstractions/base>
    
    signal (receive) peer=librechat,
    
    # MongoDB binary and data
    /usr/bin/mongod mr,
    /var/lib/mongodb/** rwk,
    /var/log/mongodb/** rwk,
    
    # System files
    /sys/kernel/mm/transparent_hugepage/enabled r,
    /sys/kernel/mm/transparent_hugepage/defrag r,
    /etc/lsb-release r,
    /etc/os-release r,
    /proc/*/mounts r,
    /sys/devices/system/node/ r,
    /sys/devices/system/node/node*/meminfo r,
    
    # Network access
    network inet stream,
    network inet6 stream,
    capability net_bind_service,
    
    # Additional required permissions
    /run/mongodb/** rwk,
    /tmp/mongodb-27017.sock rw,
    /proc/*/fd/ r,
    /proc/*/net/ r,
    /proc/*/stat r,
    /proc/version r,
    /proc/*/mountinfo r,
    /proc/sys/net/ipv4/tcp_fastopen r,
    /sys/fs/cgroup/memory/memory.limit_in_bytes r,
    
    # SSL/TLS configuration
    /etc/gnutls/config r,
    /etc/ssl/openssl.cnf r,
    /usr/lib/ssl/openssl.cnf r,
    
    # Socket creation
    /tmp/mongodb-27017.sock rw,
    capability mknod,
    /proc/version r,
    /proc/*/mountinfo r,
    /proc/sys/net/ipv4/tcp_fastopen r,
    /sys/fs/cgroup/memory/memory.limit_in_bytes r,
    
    # SSL/TLS configuration
    /etc/gnutls/config r,
    /etc/ssl/openssl.cnf r,
    /usr/lib/ssl/openssl.cnf r,
    
    # Socket creation
    /tmp/mongodb-27017.sock rw,
    capability mknod,
    
    # Logging
    /var/log/apparmor/ rw,
    /var/log/apparmor/mongodb.log w,
    
    # Debugging
    audit deny /var/lib/mongodb/** w,
    audit deny /var/log/mongodb/** w,
  }

  profile meilisearch flags=(attach_disconnected,mediate_deleted) {
    #include <abstractions/base>
    
    signal (receive) peer=librechat,
    
    /usr/bin/meilisearch r,
    /var/lib/meilisearch/** rwk,
    network tcp,
  }

}
